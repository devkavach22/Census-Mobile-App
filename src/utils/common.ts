import { getStorageData, STORAGE_KEYS } from './storage';

const USER_ROLE_API_MAP: any = {
  enumerator: 'Enumerator',
  'District Admin': 'district_admin',
  'Super Admin': 'national_admin',
};

export { USER_ROLE_API_MAP };
